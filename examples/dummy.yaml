name: dummy test

messages:
  hello: "+hello world"
  some_data: "+some data"
  neg: "-123neg"

actions:
  - execute: Recv
    message: hello
  - execute: Recv
    message: neg
  - execute: Send
    message: some_data
